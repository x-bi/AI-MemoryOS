# Skill Spec: memory-curator

## Trigger

用户明确要求复盘、沉淀经验、更新 Memory OS、生成 proposal。

## Do Not Trigger

普通编码、解释、排错、review。

## Output

只生成 `proposals/pending/` 草稿，不直接修改正式 rules / router / skills / evals。