# Change Companions

This file is the single source of truth for Memory OS formal-rule write companions.

Before writing any matching `Trigger Path`, read this file and complete the matching
`Required Companions` in the same task. Missing companions mean the formal change is
temporary or incomplete unless the user explicitly accepts the drift risk.

Source proposal: `proposals/accepted/2026-06-17-路由纠正-正式规则文件改动应由-ai-自动按-companion-映射同步关联文件.md`.

## Companion Map

| Trigger Path | Required Companions | Forbidden Actions | Required Verification | Mechanism / Timing | Reference / Exception |
|---|---|---|---|---|---|
| `router/intent-map.md`, `router/domain-map.md`, `router/workflow-map.md`, `router/skill-map.md` | `logs/router-changelog.md`; `evals/router-test-cases.md` or `evals/skill-trigger-test-cases.md` positive and negative cases | Do not edit router files and declare done without changelog and eval coverage; do not leave eval updates for the user to discover | Check changelog and eval changed in the same task; if there is a real exception, state the missing companion and reason | Manual; read before write; same task | `logs/router-changelog.md` record principle: every router change should add eval case |
| `skills/<skill>/SKILL_SPEC.md`, `skills/<skill>/references/**`, `skills/registry.json` | Run `tools/sync-skills.ps1`; run `tools/validate-memory-os.ps1` after sync; write `logs/skill-changelog.md` | Do not hand-edit `adapters/*/skills/*/SKILL.md` or `adapters/*/skills/*/references/**`; do not skip sync/validate and declare done; do not run git operations unless explicitly requested | Report sync and validate results; verify adapter generated files were updated by script, not hand-written | Both; read before write; same task; validate immediately after sync | Migrated from adapter gate Cross-Adapter Sync skill source rule |
| `adapters/gate-source/**`, `adapters/codex/templates/bootstrap.md.tmpl`, `adapters/codex/templates/gate.md.tmpl`, `adapters/claude/templates/bootstrap.md.tmpl`, `adapters/claude/templates/CLAUDE.md.tmpl` | Run `tools/sync-adapter-gates.ps1`; run `tools/sync-adapter-gates.ps1 -Check`; run `tools/validate-memory-os.ps1`; write `logs/memory-changelog.md`; update adapter docs/external-config when runtime or restore semantics change | Do not hand-edit generated adapter gate/bootstrap targets; do not skip sync/check/validate and declare done; do not put model-specific overlays into shared source | Report sync/check/validate results; verify generated targets changed by script; confirm adapter-specific overlays render only for their adapter | Both; read before write; same task; validate immediately after sync | Source proposal: `proposals/accepted/2026-06-17-adapter-gate-bootstrap-改为源文件生成并禁止直接改生成物.md` |
| Generated adapter gate/bootstrap targets: `adapters/codex/bootstrap.md`, `adapters/codex/gate.md`, `adapters/claude/bootstrap.md`, `adapters/claude/CLAUDE.md` | Move the intended change back to `adapters/gate-source/**` or the matching template, then run `tools/sync-adapter-gates.ps1`, `tools/sync-adapter-gates.ps1 -Check`, and `tools/validate-memory-os.ps1`; write `logs/memory-changelog.md` for rule changes | Do not edit generated targets by hand and call the formal rule change complete | `tools/sync-adapter-gates.ps1 -Check` must report `OK adapter-gates`; inspect generated marker and target path | Script; same task | Generated adapter gate/bootstrap boundary |
| `adapters/mcp/tool-policy.md`, `adapters/mcp/allowed-ops.md` | Update MCP safety description in both `adapters/claude/external-config.md` and `adapters/codex/external-config.md`; write `logs/memory-changelog.md` | Do not change MCP policy without updating adapter external recovery snapshots | Check both external-config MCP safety descriptions | Manual; read before write; same task | Migrated from adapter gate Cross-Adapter Sync MCP safety rule |
| Shared tool or MCP config sections in `adapters/claude/external-config.md` or `adapters/codex/external-config.md` | Synchronize the corresponding section in the other external-config file; write `logs/memory-changelog.md` when recovery semantics change | Do not allow long-term drift between shared external config snapshots | Compare corresponding sections; confirm local private differences are not accidentally synchronized | Manual; read before write; same task | Migrated from adapter gate shared external config rule |
| `proposals/pending/*.md` -> `proposals/accepted/*.md` | Apply this proposal-promotion row; preserve `source_episode`; update target files; write required changelog; if it fixes routing drift, update router correction eval | Do not move a proposal without landing target files and logs; do not drop `source_episode` | Check accepted frontmatter, target files, required logs, and related evals | Manual; during promotion | `workflows/proposal-promotion.md` reads this row for Required Logs |
| `core/memory-rules.md`, `core/safety-rules.md`, `GOVERNANCE.md` | If `README.md`, `_index.md`, or adapter gates reference the changed section, update those references; write `logs/memory-changelog.md` | Do not change core rules while leaving entry points, indexes, or references stale | Search changed section titles and path references; check whether each reference needs an update | Manual; read before write; same task | Governance/core rule reference drift boundary |
| `logs/README.md` sections that define log types, record principles, changelog destinations, or audit record semantics | If `core/memory-rules.md`, `GOVERNANCE.md`, `core/change-companions.md`, or `workflows/proposal-promotion.md` reference the changed semantics, update those references; write `logs/memory-changelog.md` | Do not change log directory semantics while core/governance/workflow rules still point to old semantics; do not treat individual changelog entries as rule sources | Search changed log names, record principle keywords, `Required Logs`, `留痕`, and `审计记录`; decide whether the edit is semantic or only wording/format | Manual; read before write; same task; typo/format-only edits may report no outward companion | `logs/README.md` is a log rule explainer, not a normal changelog endpoint |
| `core/change-companions.md` | Write `logs/memory-changelog.md`; if Trigger Path, completion standard, Forbidden Actions, Required Verification, or exception semantics change, add/update Write Companions eval; if proposal promotion row changes, update `workflows/proposal-promotion.md` reference | Do not change the companion map without recording source and impact; do not let gate/workflow pointers drift from map semantics | Check changed row references still point to real sources; check whether gate/workflow trigger pointers need updates; check eval coverage for semantic changes | Manual; read before write; same task | Self-maintenance rule for this map |
| `workflows/proposal-promotion.md` sections about Required Logs or companion sync | Update the proposal-promotion row in `core/change-companions.md`; write `logs/memory-changelog.md` | Do not duplicate full companion details in both workflow and map | Check workflow keeps only trigger/read behavior and does not copy the full companion table | Manual; same task | Avoid dual maintenance between promotion workflow and companion map |

## Deferred Companions

If the user explicitly says to change only one formal rule file for now, still read this
file first. Report every matching `Required Companion` that remains undone and mark the
result as temporary or incomplete. Do not call the formal rule change complete unless
the user confirms they accept the drift risk.
