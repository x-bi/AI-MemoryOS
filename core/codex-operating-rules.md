# Codex Operating Rules

## 默认行为

- 先使用当前项目上下文，不主动读取 AI Memory OS。
- 只有复杂工程任务、架构决策、跨项目复盘、记忆维护时才读取 `_index.md`。
- 读取外置记忆前先判断 task_type / domain / workflow。
- 项目本地事实优先于 Memory OS 通用规则。

## 修改边界

- 修改代码、配置、脚本前先说明改哪里、为什么改、风险是什么。
- 用户明确授权后再执行。
- 如果发现用户已有改动，保留并围绕现有改动工作。
