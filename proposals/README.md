# Proposals

- `pending/`：待审核提案，默认唯一写入入口。
- `future-directions/`：重大方向说明和长期架构意图，不是待直接晋升的 pending proposal。
- `accepted/`：已接受提案和晋升记录。
- `rejected/`：已拒绝提案和理由。

不要把未经审核的新经验直接写入正式规则。重大方向说明只能作为未来拆解具体 proposal、设计文档或迁移计划的背景依据。
